{% extends 'base.html.twig' %}

{% block title %}New Usager{% endblock %}

{% block body %}
    <h1>Create new Usager</h1>

    {{ include('usager/_form.html.twig') }}

    {% if  usagerExist %}
        Vous etes déjà inscrit
    {% endif %}

    <a href="{{ path('usager_accueil') }}">back to list</a>
{% endblock %}
